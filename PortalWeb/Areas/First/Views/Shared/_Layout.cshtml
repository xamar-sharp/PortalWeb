<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewData["Title"]</title>
</head>
<body>
    <div>
        @RenderBody()
    </div>
    <div>
        @if (IsSectionDefined("Additional"))
        {
            @await RenderSectionAsync("Additional")
        }
    </div>
</body>
</html>
